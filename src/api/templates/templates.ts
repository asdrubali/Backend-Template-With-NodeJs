import {
  ITemplate,
  ITemplateSupplier,
} from "../interfaces/ITemplate.interface";

export const template_create_admin = (vars: ITemplate) => {
  return ''
};

export const template_create_user = (vars: ITemplate) => {
  return ''
};

